<table>
  <tr>
    <th>Name</th>
    <th>Faction</th>
    <th>Ability</th>
  </tr>
  <% roles.each do |role| %>
  <tr>
    <td><%= link_to role.name, role.wiki_link, {target: '_'} %></td>
    <td><%= role.faction %></td>
    <td><%= role.description %></td>
  </tr>
  <% end %>
</table>
